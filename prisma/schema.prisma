datasource db {
  provider = "postgresql"
  url      = "postgresql://botato:lucas123@localhost:5432/botato?schema=public"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id      String  @id
  balance Int     @default(0)
  tickets Int     @default(0)
  isAdmin Boolean @default(false)
}
